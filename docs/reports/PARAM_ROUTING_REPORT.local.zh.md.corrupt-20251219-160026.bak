# 本地落地报告：参数路由与统一绑定改造（newliveweb�?本文�?**本地运行 / 排错 / 改造用的落地版报告**，基�?`docs/reports/PARAM_ROUTING_REPORT.zh.md` 和当前代码状态整理而成�?你可以这样用这份文档�?- 只想“能用、能调出好画面”：�?**�?0�?�?�?2.1 节（人话版）**�?- 想排错、理解路�?参数体系：看 **�?1�?�? �?*�?- 想让 AI/自己以后改代码：�?**�?10�?1�?2.2�?2.3 节（工程/约束版）**�?---## 0) 人类视角的超简总结（只想用，不想看代码�?### 0.1 你手上的几个“大旋钮�?- **三宏旋钮：`Fusion / Motion / Sparkle`**  - Fusion：画面“有多满、多亮、多粘”�?  - Motion：画面“有多动、波纹多不多”�?  - Sparkle：画面“有多闪、多金属、多锐利/多彩色”�?- **背景层：`Liquid / Basic / Camera / Video / Depth`**  - 通过 Background �?type + 各层开关，决定谁是主角、谁在前面叠加�?- **音频驱动强度：`Audio/Controls.mixToMacros`**  - 越大 �?音乐越“接管”三宏；  - 越小 �?你手动拧宏越“说了算”�?- **自动 VJ（AIVJ）：profile + 开�?*  - 开启时，系统按节奏�?Techno profile 慢慢推三�?+ slots�?  - 你动�?slots 时，会有几秒 “hand-hold”，AI 暂停抢手�?- **overlayBudget：多层叠加“挤座位”的规则**  - 决定 Basic/Camera/Video/Depth 谁在前面、谁退后，  - 决定 PM 在多层覆盖时要不要“往后缩一点”�?### 0.2 背后的思路（非常粗略）可以把系统想成三层：1. **慢层：可保存的“姿势�?*   - `VisualState` + AIVJ �?slow bank，决定整体风格和构图�?2. **快层：跟着音乐 / 空间抖动**   - AudioControls、AIVJ accent�?D coupling、overlayBudget，决定“此刻怎么动”�?3. **呈现层：真正画出来的画面**   - 三宏 + slots �?ProjectM/Liquid/各背景层参数 �?多层混合�?一句话记忆�?**你只管三�?+ 背景�?+ mixToMacros，系统会用这些再推一大堆细节参数�?*### 0.3 出问题如何自查（人话版）- **“有音乐但画面不跟着动�?*  - 检�?AudioControls 是否 enabled�?  - 检�?`mixToMacros` 是否太小�?0.2）；  - 检�?BeatTempo 是否开启且 BPM 稳定�?- **“AI 总是改掉我刚拧好的宏�?*  - 看是否处�?MIDI lock�?  - �?AIVJ 是否开着；你手动拧宏/slots 后会有几�?hand-hold，如果觉得太短，演出后再调整�?- **“画面太�?太亮，等于没效果�?*  - 检�?ProjectM opacity、Liquid brightness/contrast、Depth opacity 是否跑到极端�?  - �?12.1 里的“亮度巡查”跑一遍，把明显会黑屏/爆白�?preset/组合记下来，演出先绕开�?- **“摄像头画面太无聊，像普通直出�?*  - 参�?12.1 �?Camera 小步骤：先通过不改算法的方式（叠加比例、overlayBudget priority、与 PM 的联动）让摄像头至少和其它层“有点互动”，中长期再重构算法�?---## 1) 核心结论（给改造与排错用｜工程视角�?- �?**唯一建议持久化的视觉抽象层：`VisualStateV2`**  - Show/Favorites/URL 导入都应基于它（`src/features/visualState/visualStateStore.ts`）；  - �?`AudioFrame` 这种运行时信号不直接写入�?- �?**参数字典的唯一来源：`paramSchema`**  - Inspector/MIDI/随机的所有参数定义都来自 `src/state/paramSchema.ts`�?  - �?`group + key �?scope + patch` �?UI 和路由�?- �?**宏的统一 writer 已落�?*  - ProjectM/Liquid 等底层视觉参数，只由 `applyMacroBankToRuntime` 写入�?  - 宏数据来�?`UnifiedAivjController`，AudioControls �?AIVJ �?controller 内部融合�?- �?**overlayBudget 已有完整 runtime 消费�?*  - `overlayBudget*` 通过 Inspector/MIDI 配置�?  - bootstrap 中的 overlayBudget 逻辑根据能量 + depth freshness 计算�?overlay layer multiplier + PM 退让；  - 完全 runtime-only，不写回 VisualState�?- ⚠️ **需要显式遵守的不变�?*  - 同一底层参数在同一时刻只允许一�?writer（宏 / overlay / depth runtime…）�?  - 慢层（VisualState + AIVJ slow bank）可持久化；  - 快层（AudioControls snapshot、accent、overlayBudget�?D coupling）只在内存�?> �?2�? 节是工程细节：数据结构、路由表、已修复缺口等。这里省略，保存在主报告中，方便 AI/你以后查�?---## 8) Audio/Controls.mixToMacros 端到端路由示例（工程 + 使用兼顾�?�?`param:audio.controls.mixToMacros` 为例，从 Inspector/MIDI 到最终画面的链路�?1. Inspector/MIDI 修改 �?调用 `applyAudioControlsPatch({ mixToMacros })`（`bootstrap.ts`）�?2. `applyAudioControlsPatch` �?`audioControls.setConfig({ mixToMacros })`�?3. 每帧：`audioControls.onAudioFrame(frame, nowMs)` 输出 `fusion01/motion01/sparkle01`�?4. `UnifiedAivjController.onFrame({... audioControls })` 内部�?`mixToMacros01` 混合 audio 派生宏和 slow bank�?5. �?~33ms：`applyMacroBankToRuntime(runtimeBank, { syncUi: false })` 把宏映射�?ProjectM/Liquid 参数�?结论：`mixToMacros` 决定“音�?vs 基础宏”的混合比例；真正写 layer 参数的只�?`applyMacroBankToRuntime` 一处�?---## 9) 舞台/实战使用提示（统一 AIVJ + 参数�?- 保持**�?快层分工**�?  - 慢层决定“这个段落长什么样”；  - 快层负责跟着节奏/空间“抖一抖”�?- **mixToMacros 的推荐范�?*�?  - 演出�?.6�?.9�?  - 调参�?.3�?.5（方便听清楚“手动”与“自动”的分界）�?- 善用 **hold + MIDI lock**�?  - 推宏或改 preset 前先开 hold / MIDI lock，确保你刚推出来的形状不会立刻被 AI 覆盖�?- **overlayBudget 用来“排座位�?*�?  - �?Basic/Camera/Video/Depth 在能量不同的时候有上前/退后的变化�?  - 不要把所有视觉逻辑都塞�?overlayBudget 里�?- **BeatTempo 是节奏“地板�?*�?  - 尽量保持开启，�?AIVJ/AudioControls/overlayBudget 都有统一的节奏参考�?---## 10) 代码级约束草案（�?AI / 工程看的，当前实现已部分满足�?只保留关键结构，完整伪代码可以让 AI 按需展开�?### 10.1 宏写入单一入口- 定义 `MacroWriteOwner = "ui" | "midi" | "aivj" | "runtime"`�?- 收集 `MacroRuntimeSource`（UI/MIDI/AIVJ slow bank/AudioControls）；- 使用 `resolveRuntimeBank(VisualState, input)`�?  - owner = midiLock ? "midi" : manualHoldActive ? "ui" : aivjEnabled ? "aivj" : "runtime"�?  - 慢层：以 AIVJ slow bank / VisualState 作为起点�?  - 快层：AudioControls 混入�?  - owner（UI/MIDI）最后有权覆盖；  - 输出唯一�?`MacroBank` �?`applyMacroBankToRuntime`�?### 10.2 overlayBudget / 3D pipeline 约束- 配置层在 `paramSchema`/Inspector�?- 运行层只生产 runtime multiplier（不�?VisualState）；- 深度/人像信号只作为调度信号，不改�?Show 的结构�?---## 11) 参数关系与推荐映射（效果设计｜给�?+ AI�?### 11.1 三宏语义（保持分工清晰）- Fusion：存在感 / 亮度 / 画面填满程度（PM presence + 部分亮度）�?- Motion：运动量 / 波动强度 / 能量驱动透明度（PM energyToOpacity、Liquid timeScale/waveAmplitude）�?- Sparkle：细�?/ 锐度 / 金属�?/ 强色彩（Liquid metallic、contrast、tint/palette 等）�?建议保持现在这种“语义分工”：**不要让三个宏都去控制所有参数，否则后期很难调�?*### 11.2 三层角色：控�?/ 状�?/ 呈现- 控制层：`Audio/Controls.*`、`overlayBudget*`、BeatTempo config、AIVJ profile/mode�?- 状态层：`VisualStateV2.*`（宏、槽位、背�?type/layers、ProjectM 状态）�?- 呈现层：AudioControls snapshot、AIVJ accent、overlayBudget multiplier�?D runtime patch、各 Layer 内部状态�?推荐方向�?- 控制层参数都出现�?`paramSchema`，统一通过 Inspector/MIDI 调参�?- 状态层尽量“瘦”，只描述姿态，不存统计量；- 呈现层完�?runtime-only，用 Diagnostics 看，不写 storage�?### 11.3 搭配建议（包含亮�?黑屏风险�?- �?`mixToMacros`�?0.8）：  - 适当减小 `AudioControls.maxDeltaPerSec` 或增�?`attackMs/releaseMs`，避免宏抖动�?  - 稍微提高 `overlayBudget.maxEnergy`，让能量更多体现在层间调度，而不仅仅�?PM/Liquid 推得很亮�?- 想要更稳�?drone/ambient�?  - 使用 drone/ambient profile + `mixToMacros �?0.3�?.5`�?  - 增大 releaseMs，让变化更慢�?  - 提高 depth priority + depthWeight�?  - 注意 PM opacity 不要长期低于 ~0.25，否则观感接近“黑屏”�?- 想要 peak rave�?  - 使用 peakRave profile + `mixToMacros �?0.7�?.9`�?  - 提高 `overlayBudgetPriorityVideo/Camera`，让人像/视频有机会“压上来”；  - 确保 BeatTempo 稳定�?  - 同时限制 Liquid brightness 在大�?0.7�?.6 的安全区，避免大面积爆白�?---## 12) �?/ �?/ 长期路线图（包含摄像�?& 亮度的计划）### 12.1 短期（演出前 1 天｜目标：稳定跑通，避免黑屏/爆白/无聊摄像头）**A. 基础检�?*- 如未安装依赖：在项目根目录运�?`npm install`�?- 本地检查：`npm run verify:check`，确认构�?类型正常�?- 有时间的话：`npm run verify:headless`，看是否有明显错误�?**B. 音频 + BeatTempo + AudioControls**- �?Audio 模块中：  - 选择一首接近演出风格的测试曲目�?  - 确认能量�?文本在动，数值合理�?- 打开 BeatTempo：观�?BPM 是否能稳定在合理范围�?- 打开 AudioControls�?  - 先设 `mixToMacros �?0.6`�?  - 暂时不要大改 attack/release，保持默认�?**C. AIVJ 行为（自�?+ 手动�?*- 打开 AIVJ，选择 profile（ambient/peakRave 等）�?- 播放音乐，观察三宏是否随节奏缓慢变化�?- 手动拧宏/slots，确认：  - 你操作后画面能稳定几秒；  - 不会马上�?AI 改掉�?**D. 准备 2�? 个“安全场景�?*- 保存至少 2�? �?Favorites/Show�?  - Ambient：Motion 较低、Fusion 中等、Sparkle 较低、PM opacity �?0.5�?.7�?  - Peak：Fusion/Motion 较高、Sparkle 中等、PM opacity �?0.7�?.9�?  - 视需要增加一�?Video/Depth 较明显的场景�?- 测试在这几个场景之间切换：三宏和背景层能正确恢复，没有奇怪的闪黑/闪白�?**E. 专门做一次“亮度巡查”（避免过黑/过亮�?*对每个安全场景，按三个层面大致检查：- ProjectM�?  - 主动看强弱段落，避免 `opacity` 长时间低�?~0.2�?  - 遇到本身就极暗的 preset，优先换 preset�?- LiquidMetal�?  - 检�?`brightness/contrast` 组合，避免长时间接近全黑/全白�?  - 如果 AudioControls 驱动造成 brightness 剧烈抖动，可以临时减�?`amountLiquid` 或缩小相关宏的作用范围�?- Depth/Overlay�?  - Depth opacity 不要低到“看不出来”（例如 <0.1）；  - overlayBudget priority 不要极端偏向某一层，以免其它层“全被吃掉”�?**F. 熟悉“紧急开关�?*- 关闭 AIVJ：只保留手动�?+ AudioControls�?- 再关�?AudioControls：完全手动模式�?- 关闭 Depth/Camera/Video，只保留 Liquid + PM（最安全的兜底）�?- 确认以上模式下都不会出现“完全看不见”的画面�?**G. 摄像头层的“快速不无聊化”（不改算法，只调耦合�?*当前 Camera 层已经接�?SceneManager / overlayBudget / 3D coupling 管线里，但如果参数很保守，看起来会像“普通直出摄像头”。短期目标是�?**不改 TS 代码** 的前提下，让它至少和其它层有一点融合感�?- �?Background 中打开 Camera 层：  - �?Camera �?`enabled` 打开�?  - 首先�?Camera �?`opacity` 控制�?**0.3�?.6**，避免直接盖�?Liquid/PM�?- 利用 overlayBudget 做“让�?上前”：  - �?Inspector 中，适度提高 `overlayBudgetPriorityCamera`，但不要远高�?Video/Depth�?  - 目标：能量高�?Camera 更容易“上前”，能量低时 Camera 退�?Liquid/Basic 背后�?- 利用现有�?Camera→PM 联动�?  - 如果 UI 里有类似 “Camera edge �?PM 透明度�?的开�?参数（例�?`cameraEdgeToPmAmount01`），先保持默认或略微增加�?  - 打开人像分割（如果支持），让镜头里有人或清晰前景边缘�?  - 观察：当人进�?离开画面或移动时，PM 是否会有轻微透明�?存在感变化�?  - 如果完全没有感觉，可以把“重�?Camera 耦合算法”记�?12.2/12.3，演出先不在这里动大手术�?- 做一组“Camera 混合场景”并保存�?  - 例如：Liquid + PM 做底，Camera 做中等透明�?overlay�?  - �?Lights/现场灯光一起试几首歌，挑出“既不无聊、又不过于抢戏”的 Camera 组合�?  - 将这些组合对应的 Favorites/Show 记好名字，演出时只在这些场景之间切换�?> 短期内，我们只通过“混合比�?+ priority + 现有联动”让摄像头不再完全直出�?> 真正改算法（更聪明的 edge/region 驱动）放到中长期�?### 12.2 中期（演出后 1�? 周｜目标：好用、好调、自动避免极端亮度）- 整理 3�? 套“风格预设”（ambient/peakRave/drone/videoVJ…）�?  - 对每套预设，记录三宏默认值、AudioControls 配置、overlayBudget、Depth/Camera/Video 参数的习惯区间；  - 特别标注“亮度安全区”和“容易黑�?爆白的组合”，方便下次避坑�?- �?Diagnostics �?Inspector 中增加：  - 整体亮度/对比度的简易指标；  - overlayBudget multiplier、AIVJ owner 状态；  - Camera/Depth 活跃度（帧率、portraitEdge/depthFresh 等）�?- 启动“单一�?writer/owner + 曝光保护”的代码重构�?  - 参考第 10 节，抽出�?owner/融合模块�?  - 设计 `clampVisualExposure(params)` 等统一的曝光安全函数，让所�?runtime 路径在进�?layer 前都经过同一套裁剪逻辑�?### 12.3 长期�?�? 个月｜目标：真正的大统一 AIVJ + 稳定曝光 + 有趣 Camera�?- 完成控制�?/ 状态层 / 呈现层的彻底拆分，并为每层写清文档约束；- 把宏、音频、空间（Camera/Depth）信号统一看作一个“控制空间”，通过 profile 描述不同风格�?- 在渲染链路的末端增加一个“全局曝光/安全滤镜”层�?  - 监测整体亮度分布，自动对极暗/极亮�?preset/组合做轻微补偿；  - 保证真正意义上的“全�?全白”只在你刻意需要时才出现�?- 针对 Camera 层：  - 设计更聪明的“人�?边缘/区域”驱动策略，�?Camera 既能�?PM/Liquid/Depth 有机耦合，又不过度依赖现场光线质量�?这些中长期工作都可以留给以后的你�?AI�?现在最重要的是�?**12.1** 做扎实，让明天的演出安全、可控，而且摄像头画面不再“直�?+ 无聊”�?---## 13) 短期优化已落地（本轮：可观测�?+ 无头验证�?本轮只做“短期目标”里最关键、改动最小的一件事�?- **�?runtime-only 的关键状态变得可观测**，让你在排错时知道：  - 当前到底是谁在写宏（Human/AI/Runtime�?  - overlayBudget 的预算分配、各�?multiplier、PM retreat target 的实时�?### 13.1 代码变更（符合实际）- Diagnostics 增加两行�?  - `Writer`：显�?`macroWriteOwner`、owner TTL、AIVJ manualHold 剩余、midiLock/AIVJ 开关状�?  - `Overlay`：显�?`energy01/depthFresh01`、active 层数量、budget scale、meanOverlayMul、PM retreat target，以及各�?multiplier（Basic/Camera/Video/Depth/ProjectM�?对应实现位置�?- `src/features/console/DiagnosticsPanel.ts`  - 新增 `updateRuntimeDebug(...)`  - 面板新增 `Writer/Overlay` 两行- `src/app/bootstrap.ts`  - �?overlayBudget runtime 逻辑中维护轻�?`overlayBudgetDiag` 快照  - �?diagnostics throttle 更新中调�?`diagnosticsPanel.updateRuntimeDebug(...)`### 13.2 验证记录（只用无头浏览器�?- 命令：`npm run verify:headless`- 结果：通过（`page-errors.log` 为空�?- 产物路径�?  - `artifacts/headless/screenshot.png`  - `artifacts/headless/diff.png`  - `artifacts/headless/trace.zip`  - `artifacts/headless/report.json`  - `artifacts/headless/browser-console.log`  - `artifacts/headless/page-errors.log`---## 14) 短期优化已落地（本轮：DOM 事件绑定统一�?本轮继续推进“短期目标”的结构性小优化：把零散�?`addEventListener/removeEventListener` 统一收敛�?`domBindings.listen()`，减少遗漏与 HMR 反复挂载导致的重复监听风险�?### 14.1 代码变更（符合实际）- `src/app/controllers/audioTransportController.ts`  - `seekSlider` �?`pointerdown/pointerup/change/blur/input` 事件改为 `listen(...)` + `uiBindings` 统一释放  - `bindAudioResumeOnGesture()` 的一次�?`pointerdown/keydown` 恢复手势改为 `listen(..., { once: true })`- `src/app/controllers/macroSlotsController.ts`：slot UI �?`click/input/change` 改为 `listen(...)`- `src/app/controllers/showConfigController.ts`：Show �?`Save/Setup` 按钮事件改为 `listen(...)`- `src/app/controllers/visualActionsController.ts`：Random/Fav/全局快捷键改�?`listen(...)`- `src/app/controllers/mediaDevicesChangeController.ts`：`devicechange` 改为 `listen(...)`### 14.2 验证记录（按你的要求：不再跑无头�?- 仅执�?TypeScript 检查：`npm run lint`- 结果：通过