FROM node:20-bookworm-slim

WORKDIR /app

ENV NODE_ENV=development

# Deps are installed on first container start into the mounted /app/node_modules volume.
CMD ["sh", "-lc", "if [ ! -d node_modules ]; then npm ci; fi; npm run dev -- --host 0.0.0.0 --port 5174 --strictPort"]
