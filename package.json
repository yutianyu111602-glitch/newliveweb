{
  "name": "newliveweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sync:presets": "node scripts/sync-presets.mjs",
    "verify:headless": "node scripts/headless-verify.mjs",
    "verify:dev": "node scripts/verify-dev.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "vite-plugin-glsl": "^1.3.0",
    "playwright": "^1.50.0",
    "pngjs": "^7.0.0",
    "@types/three": "^0.170.0"
  }
}
